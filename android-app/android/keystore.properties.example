# Copy this file to `keystore.properties` (which is gitignored) and fill your real values.
# You can also provide these via environment variables instead.

ANDROID_KEYSTORE_PATH=/absolute/path/to/release-keystore.jks
ANDROID_KEYSTORE_PASSWORD=change-me
ANDROID_KEY_ALIAS=release
ANDROID_KEY_PASSWORD=change-me
